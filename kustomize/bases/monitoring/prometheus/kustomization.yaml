namespace: monitoring
resources:
  - pvc.yaml
  - clusterrole.yaml
  - clusterrolebinding.yaml
  - serviceaccount.yaml
  - statefulset.yaml
  - service.yaml
  - ingress.yaml
configMapGenerator:
- name: prometheus-config
  files:
    - etc/prometheus.yaml