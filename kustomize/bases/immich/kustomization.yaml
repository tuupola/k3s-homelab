---
namespace: immich
resources:
  - namespace.yaml
  - ingress.yaml
  - statefulset.yaml
  - deployment.yaml
  - service.yaml

  - redis/
  - postgres/

secretGenerator:
  - name: config
    envs:
      - config.env
